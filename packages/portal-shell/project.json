{
  "name": "portal-shell",
  "projectType": "library",
  "root": "packages/portal-shell",
  "sourceRoot": "packages/portal-shell",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.ts packages/portal-shell"
      }
    }
  }
}
